<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:vm="clr-namespace:Icecream.App.ViewModels"
             x:Class="Icecream.App.Pages.SigninPage"
             x:DataType="vm:AuthViewModel"
             Title="Signin"
             Shell.FlyoutBehavior="Disabled">
    <Grid>
        <VerticalStackLayout VerticalOptions="Center"
                         Padding="20"
                         Spacing="20">
            <Image Source="ice_cream.png" 
               HeightRequest="150"
               WidthRequest="150"
               Aspect="AspectFit"/>
            <Entry Text="{Binding Email}" Keyboard="Email" Placeholder="Email"/>
            <Entry Text="{Binding Password}" IsPassword="True" Placeholder="Password"/>
            <Label Text="new User? Signup" 
               FontSize="15"
               FontAttributes="Bold"
               TextColor="DeepPink">
                <Label.GestureRecognizers>
                    <TapGestureRecognizer Tapped="Signup_Tapped"/>


                </Label.GestureRecognizers>
            </Label>
            <Button Text="Sign up" IsEnabled="{Binding CanSignin}" Command="{Binding SigninCommand}"/>
        </VerticalStackLayout>
        <ActivityIndicator IsRunning="{Binding IsBusy}"/>
    </Grid>
</ContentPage>